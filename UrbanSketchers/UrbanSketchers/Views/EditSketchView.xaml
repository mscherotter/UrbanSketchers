<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
      x:Class="UrbanSketchers.Views.EditSketchView">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="148"/>
        <ColumnDefinition Width="148"/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackLayout Grid.ColumnSpan="2">
        <Entry Placeholder="Title" Text="{Binding Title, Mode=TwoWay}"/>
        <Entry Placeholder="Description" Text="{Binding Description, Mode=TwoWay}"/>
        <Entry Placeholder="Address" Text="{Binding Address, Mode=TwoWay}"/>
        <Grid HorizontalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Text="Select File" Clicked="OnSelectFile" HorizontalOptions="Start"/>
            <Label Grid.Column="1" x:Name="FilenameLabel" VerticalTextAlignment="Center"/>
            <Button Grid.Column="2" Text="Remove" Clicked="OnRemoveFile" x:Name="RemoveFileButton" IsVisible="False"/>
        </Grid>
        <!--<Entry Placeholder="ImageUrl" Text="{Binding ImageUrl, Mode=TwoWay}" x:Name="ImageUrlEntry"/>-->
        <DatePicker Date="{Binding CreationDate, Mode=TwoWay}" HorizontalOptions="StartAndExpand"/>
    </StackLayout>
    <Label Text="Latitude" Grid.Row="1" Grid.Column="0"/>
    <Entry Grid.Row="2" Text="{Binding Path=Latitude, Mode=TwoWay, StringFormat='{0:F5}'}" Grid.Column="0" />
    <Label Text="Longitude" Grid.Column="1" Grid.Row="1"/>
    <Entry Grid.Row="2" Grid.Column="1" Text="{Binding Path=Longitude, Mode=TwoWay, StringFormat='{0:F6}'}"/>
    <Button Text="Add" Grid.Row="3" Clicked="OnAdd" x:Name="AddButton" Grid.Column="0" VerticalOptions="End"/>
    <Button Text="Cancel" Grid.Row="3" Grid.Column="1" Clicked="OnCancel" VerticalOptions="End"/>
    <Image Grid.Column="2" Grid.Row="0" Grid.RowSpan="4" x:Name="Image" HeightRequest="324"/>
    <maps:Map Grid.Column="2" Grid.Row="0" IsVisible="False" Grid.RowSpan="4" x:Name="Map" HeightRequest="324" HorizontalOptions="Fill"
              MapType="Street"  PropertyChanged="OnMapPropertyChanged" />
</Grid>