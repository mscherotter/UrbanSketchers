<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UrbanSketchers.Views.EditSketchView">
    <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackLayout Grid.ColumnSpan="2">
        <Entry Placeholder="Title" Text="{Binding Title, Mode=TwoWay}"/>
        <Entry Placeholder="Description" Text="{Binding Description, Mode=TwoWay}"/>
        <Entry Placeholder="Address" Text="{Binding Address, Mode=TwoWay}"/>
        <Entry Placeholder="ImageUrl" Text="{Binding ImageUrl, Mode=TwoWay}"/>
        <DatePicker Date="{Binding CreationDate, Mode=TwoWay}" HorizontalOptions="Start"/>
    </StackLayout>
    <Label Text="Latitude" Grid.Row="1"/>
    <Entry Grid.Row="2" Text="{Binding Path=Latitude, Mode=TwoWay, StringFormat='{0:F5}'}" />
    <Label Text="Longitude" Grid.Column="1" Grid.Row="1"/>
    <Entry Grid.Row="2" Grid.Column="1" Text="{Binding Path=Longitude, Mode=TwoWay, StringFormat='{0:F6}'}"/>
    <Button Text="Add" Grid.Row="3" Clicked="OnAdd" x:Name="AddButton"/>
    <Button Text="Cancel" Grid.Row="3" Grid.Column="1" Clicked="OnCancel"/>
</Grid>