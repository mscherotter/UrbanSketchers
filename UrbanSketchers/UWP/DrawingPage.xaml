<Page
    x:Class="UWP.DrawingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <InkCanvas Grid.Row="1" x:Name="DrawingCanvas"/>
        <InkToolbar Grid.Row="0" TargetInkCanvas="{x:Bind DrawingCanvas}" InitialControls="None">
            <InkToolbarBallpointPenButton/>
            <InkToolbarPencilButton/>
            <InkToolbarHighlighterButton/>
            <InkToolbarEraserButton/>
            <InkToolbarRulerButton/>
            <InkToolbarCustomToolButton Content="&#xE081;" FontFamily="Segoe MDL2 Assets"/>
        </InkToolbar>
    </Grid>
</Page>
